server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Necessario para SPAs com react-router: qualquer rota
    # desconhecida cai de volta no index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
